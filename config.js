window.OPERATION_SWOLE_CONFIG = {
  // For GitHub Pages, set this to your deployed backend URL, for example:
  // coachApiUrl: 'https://operation-swole-coach.your-subdomain.workers.dev/api/coach',
  // The GitHub Pages workflow can replace this automatically using the COACH_API_URL repo variable.
  coachApiUrl: null,
};